@{
    var regionName = 1;
}
<div>
    <ul>
        @foreach(var round in ViewBag.Rounds)
        {
            regionName = round.Name;
            <li>@Html.ActionLink("Round " + regionName, "MatchUps", "Admin", new { round.ID }, null)</li>
        }
    </ul>
</div>